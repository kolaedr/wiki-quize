# Етап 1 — Ядро (без UGC)

**Мета:** наскрізна вертикаль — теми «Країни» і «Автобренди» імпортуються в БД, п'ять стартових ігор граються на телефоні без скролів як PWA. Плюс swipe-binary і higher-lower, EN-мова, мінімальна адмінка.

**Стартові ігри (запуск каталогу):**

1. Прапор ↔ Країна
2. Герб → Країна
3. Мова ↔ Країна
4. Автобренд ↔ Логотип
5. Автобренд → Країна походження

## 1.1. Скаффолд і інфраструктура

- [x] Next.js (App Router, TS, Tailwind) + деплой на Vercel з першого дня
- [x] Neon Postgres через Vercel Marketplace, env-и в проєкті
- [x] Drizzle + drizzle-kit: конфіг, перша міграція, npm-скрипти (`db:generate`, `db:migrate`, `db:studio`)
- [x] Better Auth: email+password + Google OAuth, таблиці в нашій БД, ролі `user|admin`
- [x] Auth UI: сторінка `/auth` (вхід/реєстрація, Google опційно через `NEXT_PUBLIC_AUTH_GOOGLE`), кнопка акаунта/виходу в хедері
- [x] Базовий layout: `100dvh` app-shell, safe-area, next-intl (EN)
- [x] **UI-кіт shadcn-стилю** (`components/ui/*`: Button/Input/Badge/Progress/Skeleton/Tabs на cva+radix, мапнуті на наші токени; реєстр shadcn з пісочниці недоступний — компоненти написані вручну 1:1 за патерном, `npx shadcn add` далі працює через components.json)
- [x] **Десктоп-лейаут**: контейнери max-w (3xl каталог/кабінет, 4xl адмінка, lg гра), гріди sm:grid-cols-2 для категорій/ігор, md:grid-cols-2 у кабінеті, рівні до 8 колонок
- [x] PWA: manifest + іконка
- [x] PWA: service worker (`public/sw.js`: cache-first статика, network-first сторінки з фолбеком)

## 1.2. Схема БД

- [x] Таблиці: `topics`, `topic_entities`, `games`, `sessions`, `import_jobs`, `limits`, `challenges` (+ auth-таблиці Better Auth); всі текстові поля — jsonb per-locale
- [x] Типи TS зі схеми
- [x] Сід-скрипт **`npm run db:seed`**: дефолтні `limits` + рукописні дані (38 країн, 26 автобрендів з лейблами EN/UK, мовами, населенням, площею) + **усі 7 стартових ігор** — для відточування дизайну без живого імпорту; ідемпотентний, живий імпорт його перезаписує

## 1.3. Інджест (TS)

- [x] Клієнт SPARQL з тротлінгом і User-Agent (`src/lib/wikidata/sparql.ts`)
- [x] Пресети тем: **Країни** (P41 прапор, P237 герб, P37 мови, P1082 населення, P2046 площа, P30 континенти), **Автобренди** (P154 лого, P495/P17 країна, P571 заснування) — `src/lib/ingest/presets.ts`
- [x] Імпорт-раннер: нормалізація, фільтри, difficulty_score (перцентиль sitelinks), запис у БД, `import_jobs` — `src/lib/ingest/run.ts` (спрощений: один прохід; чанки з курсором — TODO для великих UGC-тем)
- [x] API-роут запуску: `POST /api/admin/import` під `ADMIN_TASK_SECRET` (тимчасово, до адмінки з ролями)
- [x] Фільтри якості: sitelinks-поріг, лейбли в усіх активних мовах, обов'язкові поля, дедуп
- [ ] Фільтри: precision дат ≥ рік, preferred rank
- [ ] Pageviews-збагачення (зараз difficulty тільки з sitelinks)
- [ ] `image_credit` через `imageinfo`
- [x] Звіт валідації теми (fetched/accepted/dropped/покриття полів) → `topics.validation_report`
- [x] Vercel Cron: щотижневий ресинк усіх пресетів (`vercel.json` → GET /api/admin/import під `CRON_SECRET`)

## 1.4. Механіки і колоди

- [x] Типи `ChoiceCard`/`DeckEntity`/`BuildChoiceOptions` (`src/lib/deck/types.ts`)
- [x] Server-side `buildChoiceDeck(seed)`: seeded RNG (mulberry32), дистрактори-сусіди за складністю, ramp легкі→складні (`src/lib/deck/build.ts`)
- [x] **Правило мультизначності**: `answerKeys` — дистрактор не може ділити жоден ключ відповіді з питанням
- [x] Механіка `choice` з ТРЬОМА лейаутами: **single** (одна картка-твердження «Це прапор X» + картинка, свайп право=так/ліво=ні), **duel/double** (пара гральних карт «в руці»), **quad/multi** (промпт + 2×2 тапи); правдивість single-тверджень покрита тестом
- [x] Лейаут — **глобальне налаштування юзера** (zustand persist, перемикач single/double/multi у хедері гри, швидка зміна; міграція в акаунт — етап 2), одна гра = кілька відображень; graceful fallback якщо лейаут недоступний для гри
- [x] Input-aware ввід: `(pointer: coarse)` → drag-шар, інакше клік/hover/клавіші
- [x] Підключити ігри до БД: `/play/[slug]/[level]` будує колоди з `topic_entities` за механікою гри (`from-db.ts`), імпорт автостворює published-ігри пресетів (7 шт.)
- [x] **Relation-ігри** (`buildRefChoiceDeck`): «Мова і країна», «Звідки бренд авто» — відповідь = пов'язана сутність `{qid, labels}`; працюють на сідах
- [ ] Enrichment-прохід у живому інджесті: конвертувати QID-масиви P37/P495 у `{qid, labels}` (друга SPARQL-вибірка лейблів) — до того relation-ігри наповнюються тільки сідами
- [x] `swipe-binary` («Правда чи ні»): одна картка-твердження, свайп праворуч = правда, ліворуч = ні; бейджі вердикту при драгу, кнопки ✗/✓ для миші; шаблони тверджень через i18n (`game.tmpl.*`), генерація з числових пар з розривом ≥15%, правдивість перевірена тестом
- [x] `higher-lower` («Більше: населення»): шаблонне питання + дуель-пара карток, рендериться SwipeDuelBoard
- [x] Шаблони питань — через i18n messages (`game.tmpl.*`, EN+UK), сервер віддає ключ+параметри, клієнт рендерить локаллю

## 1.5. Ігровий флоу

- [x] Екран гри `/play`: один в'юпорт, 3 життя, стрік з множником, прогрес-бар, анімації motion
- [x] Фідбек після відповіді: підсвітка правильної/неправильної + лінк на вікі
- [ ] Пояснення (extract з REST summary) і credit картинки по тапу
- [x] Екран результату + рестарт
- [x] Запис сесій у БД: `POST /api/sessions` (score, answers, userId якщо залогінений; гість — null), інкремент plays_count
- [x] Гостьовий прогрес у localStorage: пройдені рівні + best scores (`wq-progress`)
- [x] Головна = **категорії** (published-теми з лічильником ігор), ігри всередині категорії `/category/[slug]`; порожня БД → демо-тізер
- [x] **Пагінація** як правило для всіх списків з БД: `Pagination`-компонент (prev/next, `?page=`), застосовано в категоріях і кабінеті; PAGE_SIZE=10
- [x] Кабінет `/me` (тільки для залогінених): профіль, статистика (зіграно/сума очок/місце), топ-5 найкращих результатів по іграх, лідерборд топ-10, останні ігри з пагінацією, вихід; чіп юзера в хедері веде в кабінет
- [x] Фікс 404 рівнів: рівні нарізаються по **eligible**-сутностях гри (з потрібним полем/refs/числом), а не по всіх сутностях теми
- [x] Дуель-борд рендерить `prompt.image` (лого бренда над назвою у car-origin)
- [x] Іконки: lucide-react замість емодзі/символів по ВСЬОМУ UI, включно з обкладинками ігор (`games.style.icon` → `GameIcon`-реєстр; каталог, мапа рівнів, адмінка) — жодних емодзі в інтерфейсі
- [x] **Валідація URL картинок**: `filterWorkingUrls` (HEAD з фолбеком на GET, конкурентність) — у сідах (лого брендів: кілька кандидатів на файл, береться перший робочий; биті герби відкидаються) і в живому імпорті (биті картинки → drop поля, entity без required-image → drop; лічильник brokenImages у звіті)

## 1.6. Адмінка (мінімум етапу)

- [x] `/admin` під супер-адміном (role=admin або email в `ADMIN_EMAILS` — bootstrap), лінк-щит на головній
- [x] Теми: список зі статусом/кількістю сутностей/датою синку, **імпорт/ресинк кнопкою** (server action, не curl), звіт покриття полів
- [x] Ігри: список зі статусом/рівнями/лічильником плеїв, publish/unpublish кнопкою; створюються автоматично при імпорті
- [x] Джоби: останні 10 зі статусами
- [ ] Джоби: ретрай, детальний лог
- [ ] Створення довільної гри адміном формою (тема + механіка + конфіг) — поки тільки автогенеровані

## 1.7. Рівні складності (проходження)

- [x] Ігри діляться на рівні по `perLevel` (20) айтемів за difficulty_score: рівень 1 = найвідоміші
- [x] Мапа рівнів `/play/[slug]` (лок/зірка/чек), гра рівня `/play/[slug]/[level]`
- [x] Прогрес-гейт: наступний рівень відкривається після проходження попереднього (вижив = пройшов); гостьовий прогрес у localStorage (`wq-progress`), міграція в акаунт — етап 2
- [x] Дистрактори беруться з усього пулу теми, питання — тільки зі слайсу рівня
- [ ] Нагороди/бейджі за повне проходження гри (етап 2, разом з колекціями)

## Вихід етапу

Прод на Vercel: гість ставить PWA на телефон, відкриває каталог і грає всі 5 стартових ігор (прапори, герби, мови, автолого, країни-виробники) англійською, без скролів; бонусом «правда/брехня» і «більше/менше» на тих самих темах. Адмін імпортує нову тему пресетом і збирає з неї гру формою.
