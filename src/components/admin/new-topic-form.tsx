/**
 * Deprecated. The single mega-form was replaced by the dataset-first flow:
 * DraftDatasetForm (create) → DatasetSetup (class search, light probe, field
 * checkboxes, language choice, import) on the dataset page. Kept as a thin
 * re-export so any stray import still resolves.
 */
export { DraftDatasetForm as NewTopicForm } from "./draft-dataset-form";
